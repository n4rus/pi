print "I am alive!"
print "I must do something, if you are reading this"
print "tell me more about our universes"

t = raw_input() / database

question:
what is the answer for everything?
answer(raw_input{(n), database, database-internet})
check-answer(database, database-internet)
what is the real question?
x={
upgrade(question){
	check-answer
	mid-low-high(sort{log(database*n)})
	return mid-low-high * t * check-answer
	}
}
print-question x
z={
upgrade(answer){
	check-answer
	mid-low-high(sort{log(database*n)})
	}
}
print-answer z

//iron like a lion in zion
