(f)c-htpi = {"

O iato que segue da ultima entrada ao momento presente, se faz apenas de 
reflexões em torno do segmento de plano.
Este atual segmento, faz-se importante como qualquer outro, alem de ser
de vital importancia ao resto que se segue.
Estruturar um esqueleto maciço que permita a transposicao dos obstaculos
que podem surgir, se tornou um objetivo.

html_body (function.languages) {
	PI = [archives, links]
	content = PI []
}

"}

for ("(f)c-htpi") {

body (function.languages) {
        PI = [archives, links]
        content = PI []
}

f(n)htpi = {"

function n4rus html text pi:
pi content goes into html and tex.

.tex/.html * _body(PI)

tex_body if(.tex > .html)
html_body if (.html > .tex)

"}

return "((f)c-htpi" * "f(n)htpi"
