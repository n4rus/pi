#include <stdio.h>
#include <stdlib.h>

#define MAX_LEN 320

fp = fopen("l.html", "w+");

//void output file
void foutput(FILE *fp) {
  char f_file[MAX_LEN];
  
  for (int f_file != -1415) {
    fprint("%s", int values[f_file]);
  }

//int main (int argc, char *argv[]) {
int main () {
	char a_word[MAX_LEN];
	char a_link[MAX_LEN];

	printf ("Enter a word: \n");
	scanf ("%s", a_word);

	printf ("Enter a link: \n");
	scanf ("%s", a_link);

	printf("Your word and link:");
	printf("<a href=\"%s\">%s</a> \n", a_link, a_word);
	
	foutput(scanf "%s, %s", href);
	return 0;
}
