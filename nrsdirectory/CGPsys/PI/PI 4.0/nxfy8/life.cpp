//o formato rouba a ideia, colocando-a num molde;
//por conta disto, perdi o insight a primeiro modo, consumindo mais tempo.
#include <iostream>
#include <cmath>
using namespace std;

//for each function, a vector char*value class is used
float xp(char, float); //xp total
float xpc(char, float); //class xp total
void task(char, float, float, float); //class, xp hour, time expent, task xp;
void cost(char, float, float, float); //class, task, time, cost;

//cost is how we alter the bytes, the calculus should be based on physical
//math, relying on eletronic i/o devices, distributing the energy while 
//improving the web
